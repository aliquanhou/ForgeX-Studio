/** ForgeX Studio v0.2 — AI Engineering Command Center.
 *
 *  Phase 0.5: Mock-driven three-column layout.
 *  Replace useMockStream() with real SSE connect() when ready.
 */
import { ForgeShell } from './layout/ForgeShell'

export default function App() {
  return <ForgeShell />
}
